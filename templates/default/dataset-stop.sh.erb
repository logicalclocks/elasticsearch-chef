#!/bin/bash
river=dataset
/usr/local/elasticsearch-jdbc-<%= node[:elastic][:jdbc_river][:version] %>/bin/kill-process.sh $river /usr/local/elasticsearch-jdbc-<%= node[:elastic][:jdbc_river][:version] %>/rivers
