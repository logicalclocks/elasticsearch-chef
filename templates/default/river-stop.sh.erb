#!/bin/bash
river=parent
/usr/local/elasticsearch-jdbc-<%= node[:elastic][:jdbc_river][:version] %>/bin/kill-process $river /usr/local/elasticsearch-jdbc-<%= node[:elastic][:jdbc_river][:version] %>/rivers
