#!/bin/bash

export ES_PATH_CONF=<%= node['elastic']['base_dir'] %>/config
export ES_HOME=<%= node['elastic']['base_dir'] %>

<%= node['elastic']['base_dir'] %>/bin/kill-process.sh Elasticsearch <%= node['elastic']['pid_file'] %>
